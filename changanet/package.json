{
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix changanet-backend\" \"npm run dev --prefix changanet-frontend\"",
    "dev:backend": "npm run dev --prefix changanet-backend",
    "dev:frontend": "npm run dev --prefix changanet-frontend"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "nodemailer": "^7.0.6"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
